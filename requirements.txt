# 企业知识库问答系统 - 后端依赖
# 建议 conda 环境：langchain（Python 3.13）
# 安装: pip install -r requirements.txt

# ---- Web / API ----
fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.12

# ---- 数据库 ----
sqlalchemy>=2.0.49
alembic>=1.14
psycopg2-binary>=2.9
pgvector>=0.3.6

# ---- 文档解析 ----
pypdf>=5.0
python-docx>=1.1

# ---- 配置 ----
pydantic-settings>=2.7
python-dotenv>=1.0

# ---- LLM（DeepSeek OpenAI 兼容接口）----
openai>=1.55
httpx>=0.27

# ---- 检索（关键词 BM25 + 多路召回 RRF）----
jieba>=0.42
rank_bm25>=0.2.2

# ---- 登录鉴权（图片验证码绘制）----
# 需要 >=10.1：ImageFont.load_default(size=...) 自该版本起才接受字号参数
pillow>=10.1

# ---- 测试 ----
pytest>=8.3
pytest-asyncio>=0.24
