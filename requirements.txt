# 企业知识库问答系统 - 后端依赖
# 建议 conda 环境：langchain（Python 3.13）
# 安装: pip install -r requirements.txt

# ---- Web / API ----
fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.12

# ---- 数据库 ----
sqlalchemy>=2.0.49
alembic>=1.14
psycopg2-binary>=2.9
pgvector>=0.3.6

# ---- 文档解析 ----
pypdf>=5.0
python-docx>=1.1

# ---- 向量化 / 模型 ----
sentence-transformers>=3.3
modelscope>=1.30    # 国内下载 BGE 模型（HF 直连常不可用）

# ---- 配置 ----
pydantic-settings>=2.7
python-dotenv>=1.0

# ---- LLM（DeepSeek OpenAI 兼容接口）----
openai>=1.55
httpx>=0.27

# ---- 测试 ----
pytest>=8.3
pytest-asyncio>=0.24
